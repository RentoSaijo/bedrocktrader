% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/versions.R
\name{bedrock_versions}
\alias{bedrock_versions}
\title{List Supported Minecraft Bedrock Versions}
\usage{
bedrock_versions()
}
\value{
A base data frame with one row per supported release, ordered from
newest to oldest:
\itemize{
\item \code{version} (\code{character}) is the stable Bedrock version accepted by the
\code{version} argument in the other package functions.
\item \code{release_date} (\code{Date}) is the release date recorded in Mojang's
stable-version registry.
\item \code{latest} (\code{logical}) identifies Mojang's current latest stable release.
}
}
\description{
Lists the stable Minecraft Bedrock releases that \code{bedrocktrader} can read.
The registry is retrieved from Mojang each time the function is called, so
the result grows as new supported stable releases appear.
}
\details{
Supported releases begin with version \verb{1.26.20.4}. Preview releases do not
appear in the result. Passing \code{version = "latest"} to another package
function selects the row where \code{latest} is \code{TRUE} at the time of that call.

This function performs a fresh online retrieval and does not read or write a
cache.
}
\examples{
\dontrun{
versions <- bedrock_versions()
versions

versions[versions$latest, ]
}
}
